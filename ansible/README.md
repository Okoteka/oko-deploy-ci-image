# CI Docker Image

This Docker image is used by the oko-deploy project for GitLab CI/CD pipelines.

## Includes
- Python 3.11 (base for ansible)
- Ansible 
- Docker CLI
- SSH client + sshpass
- SOPS
- Task (task runner)
